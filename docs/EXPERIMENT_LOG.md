# Experiment launch log

Human-readable ledger of every paper experiment run launched, across all
machines. This is a process/coordination aid (multiple machines and people
can launch runs against the same shared `METHYL_DATA_ROOT`) — it is not a
substitute for the per-run `metadata.json` (which already records `host`,
`git_commit`, `created_at_utc` and the full resolved config) or for
`configs/paper_studies.yaml` (the frozen arm/seed matrix). See
[`PAPER_EXPERIMENTS.md`](PAPER_EXPERIMENTS.md) for the experiment catalog
this log's `study`/`arm` columns refer to.

**Add one row here whenever you launch a run against the shared paper study
surface** (`configs/paper_studies.yaml`), before or immediately after
starting it — see the logging rule in `CLAUDE.md`.

| Date (UTC) | Study | Arm | Seed | Run ID | Machine | Status | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-12 | main_model | main | 17 | main-seed17 | kingkong | superseded | crashed before first checkpoint; superseded by main-seed17-r3 |
| 2026-09-12 | main_model | main | 42 | main-seed42 | kingkong | superseded | crashed before first checkpoint; superseded by main-seed42-r3 |
| 2026-09-12 | main_model | main | 123 | main-seed123 | kingkong | superseded | crashed before first checkpoint; superseded by main-seed123-r3 (relaunched 2026-09-13 via the GPU-free watcher, using --locus-store) |
| 2026-09-13 | main_model | main | 17 | main-seed17-r3 | kingkong | done | 80/80 epochs, evaluated, paper/record.json written. Study label corrected `main` -> `main_model` 2026-09-13 to match what was actually recorded (configs/paper_studies.yaml renamed to match) |
| 2026-09-13 | main_model | main | 42 | main-seed42-r3 | kingkong | done | 80/80 epochs, evaluated, paper/record.json written |
| 2026-09-13 | main_model | main | 123 | main-seed123-r3 | kingkong | running | relaunched via GPU-free watcher once the git tree was clean; 8/80 epochs as of 2026-09-13 18:2x |
| 2026-09-13 | functional_representation | basic_context | 17 | functional_representation__basic_context__seed17 | ekko | running | restarted via `scripts/run_functional_representation_local.sh` (bypasses `paper_worker.py`'s shared campaign; single-seed only per user direction 2026-09-13 -- secondary/supplementary studies get seed 17 for now, not the full 3-seed matrix). Reads chr1_runtime inputs from a local /mnt/hdd mirror instead of the sshfs dune mount, after a transient concurrent-write failure there. Along the way fixed two repo-wide bugs found blocking this: `classify_recipe()` not resolving `extends:` (976f9f8), and `MatchedChr1Protocol` missing `primary_source`/`train_cpg_idx`/`val_cpg_idx` after 695043e's generalization, which broke all chr1 training (488af12) |
| 2026-09-13 | functional_representation | full_functional | 17 | (reuse main-seed17-r3) | kingkong | deferred, not retrained | `full_functional` is `configs/models/main.yaml` seed 17 -- identical recipe/seed/scope to `main-seed17-r3` already running on kingkong. Dropped from ekko's local queue to avoid duplicating ~16h of GPU work; once main-seed17-r3 completes, generate the `functional_representation/full_functional/seed17` record from that checkpoint (evaluate+record only, no retraining) |
| 2026-09-13 | functional_representation | minimal | 17 | functional_representation__minimal__seed17 | ekko | queued | via a one-shot watcher waiting for basic_context/seed17's train.py (pid) to exit, then launching directly with `scripts/paper_experiment.py --profile configs/data/paper_chr1_local_ekko.yaml` (the sequential wrapper script's own loop process was killed while removing full_functional; basic_context/seed17 itself kept running unaffected) |
| 2026-09-13 | main (chr123, not in paper_studies.yaml matrix) | main | 17 | chr123-main-seed17-iotuned | dune.micc | stopped, superseded | killed after ~2min to free GPU1 for a single urgent higher-priority run (chr123-main-seed17-bigbatch below); no checkpoint reached |
| 2026-09-13 | main (chr123, not in paper_studies.yaml matrix) | main | 42 | chr123-main-seed42-iotuned | dune.micc | stopped, superseded | killed alongside seed17-iotuned above, same reason; no checkpoint reached |
| 2026-09-13 | main (chr123, not in paper_studies.yaml matrix) | main | 17 | chr123-main-seed17-bigbatch | dune.micc | running | urgent single-seed run for paper writing (ENCODE experiment), requested ahead of the 3-seed matrix. Recipe `configs/models/chr123/main_chr123_bigbatch_solo.yaml` (hdf5_cache_mb 256->2048, array.cpg_size 640->2048 chunk-aligned, larger sample_size per pool since running solo on GPU1 with no second process sharing memory); same rna_cache/cpg-targets-dir/locus-store as the iotuned recipes above. Running at ~39.7/41GB GPU1 memory, 100% util -- little headroom, watch for OOM. Seeds 42/123 (and the full paper matrix) deferred until after this result. |
| 2026-09-13 | mean_proxy | full | 17 | (reuse main-seed17-r3) | kingkong | deferred, not retrained | `full` is `configs/models/main.yaml` seed 17 -- identical recipe/seed/scope to `main-seed17-r3` (done). Reuse that checkpoint for the `mean_proxy/full/seed17` record (evaluate+record only, no retraining), same pattern as `functional_representation/full_functional/seed17` above |
| 2026-09-13 | mean_proxy | no_supervision | 17 | mean_proxy__no_supervision__seed17 | kingkong | running | launched directly via `scripts/paper_experiment.py --recipe configs/models/mean_contribution/no_mean_supervision.yaml --stage all --allow-dirty`; single-seed (17) per the same secondary/supplementary-study convention as `functional_representation`, chosen to match `main-seed17-r3`'s seed for direct comparison against the `full` arm above. Running as a 2nd/3rd training process on kingkong's single GPU0 alongside `main-seed123-r3` -- expect slower per-epoch time from compute time-slicing, watch for OOM (GPU0 was ~20/49GB used before launch) |
| 2026-09-13 | mean_proxy | no_branch | 17 | mean_proxy__no_branch__seed17 | kingkong | crashed, queued for retry | crashed immediately with `CUDA OutOfMemoryError` (GPU0 had `main-seed123-r3` 19.3GB + `no_supervision` 16.9GB already resident, no room for a 3rd process on the 47GB card). Queued via a one-shot watcher (`scripts/_watch_and_launch_no_branch.sh`, PID 3355910) polling `main-seed123-r3` and `no_supervision`'s train.py PIDs every 30s; relaunches this exact command automatically as soon as either one exits and frees GPU memory |
| 2026-09-13 | cpg_prior | cpg_prior | (no_seed) | cpg_prior__cpg_prior | spyro | done | First run of this study through the current pipeline (no prior `experiments/runs/cpg_prior/` existed anywhere on the shared root). Launched via `scripts/paper_experiment.py --profile configs/data/paper_chr1.yaml --stage all --allow-dirty` on a freshly bootstrapped machine (new conda env `methyl-predictor` in `/mnt/hdd/vcivale/miniconda3`, torch 2.5.1+cu121; dune-raid data mounted read-write at `dune_data/` inside the repo, gitignored). Hit and fixed a real blocking bug along the way: `build_evaluate_command`'s `cpg_prior` branch passed `--output` as the `metrics.json` file path, but `CpGPriorEvaluator` treats `--output` as a directory it writes `metrics.json` into -- created `evaluation/chr1/metrics.json` as a directory instead of a file, breaking `build_record`. Fixed in `scripts/paper_experiment.py` (pass `output.parent`). |
| 2026-09-13 | cpg_prior (chr123, not in paper_studies.yaml matrix) | cpg_prior | (no_seed) | cpg_prior__cpg_prior | spyro | done | Ad hoc, off-matrix (same convention as the other chr123 rows above). Launched via `scripts/evaluate.py --model cpg_prior --eval-scope chr123` directly (paper_experiment.py's chr123 support is untested/unused for this study). `--feature-cache` uses a local adapter (`dune_data/derived/prior_cache_adapter/chr123`, symlinking `cpg_idx.npy`/`target_mu.npy` from `derived/cpg_statistics/chr123` -- the pure-Array 93,104-official-CpG reference -- as `prior.npy`) since no `prior.npy`-named cache exists for chr123. Deliberately NOT `derived/cpg_statistics/chr123_rna_universe`: that directory's 5.4M rows are mostly auxiliary-only placeholders with `target_mu=0`, which would violate `LocusPriorCache`'s `(0,1)` contract. |
| 2026-09-13 | functional_baselines | global_rna_shift | 17 | functional_baselines__global_rna_shift__seed17 | spyro | running | Also fixed a second blocking bug hit right after the first: `CpGPriorEvaluator.run()`'s `metrics.json` never included a top-level `"metrics"` key mirroring the headline view (unlike `evaluate_rna_checkpoint`'s summary, which does), so `build_record`'s `evaluation.metrics == views[headline_view]` check always failed for `cpg_prior`. Fixed in `src/methylation_predictor/rna_training/evaluator.py`. Both fixes verified with `python -m compileall -q src scripts` and `pytest -q -m "not slow"` (6 pre-existing failures confirmed unrelated -- same failures, same count, on a clean stash of this branch -- from the `main`->`main_model` study rename drift, not from this change). Launched via `scripts/paper_experiment.py --profile configs/data/paper_chr1.yaml --stage all --allow-dirty --resume`; single-seed (17) per the same secondary/supplementary-study convention as `functional_representation`/`mean_proxy` on ekko/kingkong. Solo on this machine's single RTX 4090 (24GB). |
| 2026-09-13 | functional_baselines (chr123, not in paper_studies.yaml matrix) | global_rna_shift | 17 | functional_baselines__global_rna_shift__seed17 | spyro | queued | Ad hoc, off-matrix. New recipe `configs/models/chr123/baseline_global_rna_shift_chr123.yaml` (extends `main_chr123_iotuned_v2.yaml`, not `main_chr123_bigbatch_solo.yaml` -- the latter's batch sizes were sized for ~40GB of solo A100 headroom and would not fit this machine's 24GB card). Will launch via `scripts/train.py` + `scripts/evaluate.py` directly (same pattern as the other chr123 rows above) once the chr1 run above finishes -- single GPU on this machine, sequential only. `--prior-cache` at evaluation time will reuse the `cpg_prior`/chr123 adapter above. |
