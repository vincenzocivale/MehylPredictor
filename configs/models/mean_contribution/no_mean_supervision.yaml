# Claim-driven ablation of configs/models/main.yaml.
# Architecture/capacity is unchanged; only L_mean is disabled.
extends: ../main.yaml

locus_cls:
  use_mean_branch: true
  aux_weight: 0.0

tracking:
  group: mean_contribution_functional_2026_09
  tags: [functional-only, mean-contribution, no-mean-supervision]
