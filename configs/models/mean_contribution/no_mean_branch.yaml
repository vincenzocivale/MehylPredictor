# Claim-driven ablation of configs/models/main.yaml.
# Removes the training-only mean-proxy head and its supervision while keeping
# every surviving parameter identically initialized under the same seed.
extends: ../main.yaml

locus_cls:
  use_mean_branch: false
  aux_weight: 0.0

tracking:
  group: mean_contribution_functional_2026_09
  tags: [functional-only, mean-contribution, no-mean-branch]
