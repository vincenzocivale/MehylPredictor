# Pathway resources

Generate `survpath_combine_tcga.npz` locally with `scripts/prepare_gene_pathway_membership.py`. It is aligned to the current canonical TCGA RNA gene axis and derived from the pinned public SurvPath pathway signature file.
